# Pinned to the versions this was trained and measured on.
# Use opencv-python instead of the headless build if you want a display.
torch==2.13.0
gymnasium==1.3.0
pygame==2.6.1
opencv-python-headless==5.0.0
numpy==2.4.6
matplotlib==3.11.1
imageio==2.37.0
